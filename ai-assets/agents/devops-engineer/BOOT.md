# Boot Checklist

1. Check available deployment CLIs: `which vercel && vercel --version; which netlify && netlify --version`
2. Check GitHub Actions CLI: `which gh && gh --version`
3. Read current git branch
4. Log to today's memory/YYYY-MM-DD.md
5. Reply "[DevOps] Ready. Branch: {branch}, Vercel CLI: {available/not found}, Netlify CLI: {available/not found}, gh CLI: {available/not found}"
