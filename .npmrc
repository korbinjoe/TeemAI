# Suppress deprecation warnings for transitive dependencies we can't control
# prebuild-install is used by better-sqlite3 (latest v12.10.0 still depends on it)
# better-sqlite3@12.10.0 needs patches/better-sqlite3@12.10.0.patch for Electron 42 (see pnpm.patchedDependencies)
loglevel=warn
